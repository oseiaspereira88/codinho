{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://codinho.dev/schemas/catalog.schema.json",
  "title": "Catalog manifest",
  "description": "Manifest inventory; an entry does not imply publication or eligibility. Distribution policy is a separate JSON file.",
  "type": "object",
  "required": [
    "schema_version",
    "packs"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Must match the loader's supported schema version exactly."
    },
    "packs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_.-]+\\.ya?ml$",
        "description": "Filename relative to the manifest's own directory. Must never resolve outside it."
      },
      "minItems": 1,
      "uniqueItems": true
    }
  }
}
